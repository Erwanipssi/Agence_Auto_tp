{% extends 'base.html.twig' %}

{% block title %}Ajouter un commentaire{% endblock %}

{% block body %}
    <h1>Ajouter un commentaire pour la réservation #{{ reservation.id }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3">Ajouter</button>
    {{ form_end(form) }}

    <a href="{{ path('app_reservation_show', {id: reservation.id}) }}" class="btn btn-secondary mt-3">Retour</a>
{% endblock %}
